# These secrets are managed via GitHub Secrets and auto-generated during deploy.
# Add them in: GitHub → Settings → Secrets and variables → Actions
#
# Required secrets:
#   - MYSQL_ROOT_PASSWORD
#   - POSTGRES_USER
#   - POSTGRES_PASSWORD
#   - REDIS_PASSWORD

# MySQL
MYSQL_ROOT_PASSWORD=change_this_strong_password

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_strong_password

# Redis
REDIS_PASSWORD=change_this_strong_password
